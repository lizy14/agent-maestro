---
"agent-maestro": patch
---

Add a new `Agent Maestro: Configure WorkBuddy Settings` command that creates or updates `.codebuddy/models.json` (user-level or project-level) to route WorkBuddy model calls through Agent Maestro's OpenAI-compatible `/api/openai/v1/chat/completions` endpoint.
